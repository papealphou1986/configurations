server:
  port: 8761
eureka:
  instance:
    hostname: 192.168.154.250 # Set to the actual IP of your server
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
