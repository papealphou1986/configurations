server:
  port: 8761
eureka:
  instance:
    address: 192.168.154.250
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.address}:${server.port}/eureka/
